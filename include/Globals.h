#pragma once

namespace Globals {

	//only used in certain necessary situations
	//eg. getting window height when calculating sprite height offset
	inline int CurrentGameHeight = 0;
	inline int CurrentGameWidth = 0;

};