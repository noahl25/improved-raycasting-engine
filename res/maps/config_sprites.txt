// begin a new sprite config with 'SPRITE'
// seperate features with a new line
// features should be declared as "feature:value"
// avaliable features:
//  color - required, color of the sprite on sprite map
//  identifier - required, custom identifier that has its creation defined in code. 
//							this allows you to create custom classes with custom 
//							behavior. derive this class from Sprite and override
//							Update(). Draw() can be overriden but it isnt reccomended

SPRITE
color:0x00ff00ff
identifier:Test

 